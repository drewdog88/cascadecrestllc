/**
 * Routes where BotID attaches verification headers (POST/PUT/etc.).
 * Add entries when you add APIs, forms, or server actions.
 * @see https://vercel.com/docs/botid/get-started
 */
export const botIdProtectedRoutes = [
  // Example:
  // { path: "/api/contact", method: "POST" },
] as const;
